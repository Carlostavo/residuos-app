
import { useState } from 'react'; import { supabase } from '../lib/supabaseClient'; import { useRouter } from 'next/router';
export default function Login(){ const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const router=useRouter();
  async function handleLogin(e){ e.preventDefault(); const { data, error } = await supabase.auth.signInWithPassword({ email, password }); if(error){ alert(error.message); return; } router.push('/'); }
  return (<div style={{display:'flex',height:'70vh',alignItems:'center',justifyContent:'center'}}><form onSubmit={handleLogin} style={{background:'#fff',padding:24,borderRadius:12,boxShadow:'0 8px 30px rgba(0,0,0,0.08)',width:360}}><h2 style={{textAlign:'center'}}>Iniciar sesión</h2><input placeholder='Correo' value={email} onChange={e=>setEmail(e.target.value)} style={{width:'100%',padding:10,marginTop:12,borderRadius:8,border:'1px solid #e2e8f0'}} /><input type='password' placeholder='Contraseña' value={password} onChange={e=>setPassword(e.target.value)} style={{width:'100%',padding:10,marginTop:12,borderRadius:8,border:'1px solid #e2e8f0'}} /><button type='submit' className='btn btn-primary' style={{width:'100%',marginTop:12}}>Entrar</button></form></div>); }
