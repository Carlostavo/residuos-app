import { useEdit } from './EditContext'
export default function EditorSidebar(){ const { showSidebar } = useEdit(); if(!showSidebar) return null; return (<aside className='editor-sidebar' aria-label='Editor sidebar'><div style={{ display:'flex', justifyContent:'space-between', alignItems:'center' }}><div><strong>Editor</strong><div className='small'>Bloques · Propiedades · Capas</div></div><div><button onClick={()=> window.dispatchEvent(new CustomEvent('editor:clearSelection'))}>Limpiar</button></div></div><div style={{ marginTop:12 }}><div style={{ display:'flex', gap:8 }}><button onClick={()=> window.dispatchEvent(new CustomEvent('editor:add',{detail:{type:'text'}}))}>Texto</button><button onClick={()=> window.dispatchEvent(new CustomEvent('editor:add',{detail:{type:'image'}}))}>Imagen</button><button onClick={()=> window.dispatchEvent(new CustomEvent('editor:add',{detail:{type:'video'}}))}>Video</button><button onClick={()=> window.dispatchEvent(new CustomEvent('editor:add',{detail:{type:'button'}}))}>Botón</button></div></div></aside>) }
